[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --workers 1 --timeout 120 --bind 0.0.0.0:$PORT --log-level info --preload"
healthcheckPath = "/api/status"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
